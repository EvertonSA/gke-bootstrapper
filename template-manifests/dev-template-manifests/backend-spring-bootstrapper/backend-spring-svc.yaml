---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "backend-spring-service"
  namespace: "dev"
  labels:
    app: "backend-spring"
spec:
  ports:
  - protocol: "TCP"
    port: 8080
    targetPort: 8080
  selector:
    app: "backend-spring"
  type: "ClusterIP"
